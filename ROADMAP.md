# BIT101-S 优化路线图

> 基线：2026-09-17 · 提交 `cb105b6`
>
> **进度：M1–M5 全部完成 · M6–M9 已完成（座位图渲染修复与列表体验优化）**

---

## 现状速览

| 能力 | 状态 |
|------|------|
| BIT101 登录 + WebView CAS 换 JWT | ✅ 可用 |
| 学校 SSO 二次验证（短信/邮件/扫码） | ✅ 可用（复用 BIT-Login SDK） |
| 座位树 / 时段 / 座位图查询 | ✅ 可用 |
| 单次预约（选座 → `confirmSeat`） | ✅ 可用（M1.2 后为唯一单次路径） |
| 取消预约 | ✅ 已修日期/时段 bug（M1.3） |
| 监控预约 / 优先预约 | ✅ 图上点选 + 轮询带退避（M2.4 / M5.2） |
| 任务列表 | ✅ 已持久化（M2.2）+ 分区与存活信息（M9） |
| 登录态持久化 | ✅ JWT 加密落盘（M2.1），冷启动免重登 |
| 后台保活 | ✅ 前台服务（M2.3），锁屏 / 退后台 / 进程重启均可续跑 |
| 预约结果通知 | ✅ 抢到座位 / 任务停止时通知（M2.3 收尾） |
| 真实房间底图 + 图上选座 | ✅ 与官方前端逐像素一致（M5.3 / M8） |
| 「我的预约」（签到时限 / 取消） | ✅ 可用（M5.4） |

> **真机联调已完成**：预约 → 取消全链路已在真机跑通（M5 收尾轮）。
> 仍待真机确认的项见文末「真机验证清单」。
>
> ⚠️ 早期「模拟器到不了 seatlib」的结论**已被推翻**（2026-09-18 实测），
> 模拟器可以用于联调 —— 详情见「风险与前提」。

---

## M1 补齐核心功能 —— 让三种模式真正可用 ✅ 已完成

> **目标**：三种预约模式都能创建任务，并在任务列表看到状态流转。这是当前最大的功能缺口。
> **已交付**：M1.1 接线 `addTask()`（监控座位号必填 / 优先可留空）、M1.2 统一为「保留座位图路径」（已删除 `executeSingleReserve`）、M1.3 修取消预约的日期与时段 bug、M1.4 时段解析下沉 ViewModel 消除竞态。

| # | 任务 | 关键改动 | 验收标准 |
|---|------|---------|---------|
| 1.1 | 接线 `addTask()` | `NewTaskScreen`：模式 ≠ 单次时显示「座位号」输入框 + 「创建任务」按钮，调用 `viewModel.addTask(...)` | 选监控/优先并填座位号后，任务出现在列表页且状态实时更新 |
| 1.2 | ~~统一单次预约实现~~ ✅ | 已决定**保留座位图路径**：删除 `executeSingleReserve()` 与 `addTask` 的 SINGLE 分支；`TaskMode.SINGLE` 保留仅用于 UI 模式选择 | 单次预约只有一条实现路径 |
| 1.3 | 修 `cancelReservation()` 日期 bug | `SeatViewModel.cancelReservation()`：用当前查看的 `day` 重载座位图，而非硬编码 `LocalDate.now()` | 在「明天」的座位图取消后，页面仍停留在明天 |
| 1.4 | 修座位图时段参数竞态 | `SeatMapScreen`：把 `segId`/`startTime`/`endTime` 纳入 `LaunchedEffect` 的 key，或等 `seatDates` 就绪后再加载 | 从任务列表直接进入座位图时，提交的时段参数正确 |

---

## M2 稳定性与登录体验

> **目标**：让 App 达到「可以日常用」的门槛。

| # | 任务 | 关键改动 | 验收标准 |
|---|------|---------|---------|
| 2.1 | ~~JWT token 持久化~~ ✅ | 已完成：新增 `SeatLoginStatus`（加密 DataStore）+ Hilt 绑定；token 的读写与落盘统一收敛在 `SeatApi`（赋值即持久化）；登出时一并清除。注：类名刻意避开 `SeatStatus` —— 该名字已被座位可用状态枚举占用 | 杀进程重开无需再走一次 WebView 登录 |
| 2.2 | ~~任务持久化~~ ✅ | 已完成：新增 `SeatTaskStore`（DataStore）+ Hilt 绑定；`ReservationTask` JSON 序列化；启动恢复、变更落盘（单写入者保证顺序）；登出清空 | 杀进程重开，任务与状态都还在 |
| 2.3 | ~~后台保活~~ ✅ | 已完成：**前台服务**（`SeatMonitorService`，`dataSync` + 常驻通知）执行轮询，新增 `SeatTaskRepository` 作为任务状态唯一持有者；服务由任务流驱动，无活跃任务时自停。**未用 WorkManager**（Doze 会推迟到分钟级，不适合秒级抢座）。收尾：`POST_NOTIFICATIONS` 运行时申请（创建任务时 + 任务列表提示条）、预约结果通知（独立 `seat_result` 渠道，点击回应用） | 退到后台 / 锁屏后监控仍继续；进程被杀后自动续跑；抢到座位有通知 |
| 2.4 | ~~轮询退避与上限~~ ✅ | 已完成：基准 10s / 5s，连续失败 2 倍退避封顶 5 分钟；单任务最长 2 小时后自动停止并置失败 | 断网时不会无限高频重试 |
| 2.5 | ~~401 自动登出加固~~ ✅ | 已完成代码层面加固：① 拦截器对**所有** 401 抛 `TOKEN_EXPIRED`（原先只在本地 token 非空时抛，本地被清空后 401 会被当成普通 HTTP 错误，自愈链路断裂）；② `SeatApi.tokenFlow` 让 token 变化可被观察 —— 此前前台服务清空 token 时 UI 仍显示「已登录」；③ UI 区分「学校账号未登录」与「学校已登录但座位未授权」；④ 会话失效时服务终止在跑任务并提示重新登录。**真机联调仍待做** | token 过期后 UI 正确回到登录态并给出提示 |

---

## M3 工程质量 ✅ 已完成

| # | 任务 | 完成情况 |
|---|------|---------|
| 3.1 | ~~加 `.gitattributes`~~ ✅ | 已加：`gradlew`/`*.sh` 强制 LF，`*.bat` 强制 CRLF，二进制文件标记 `binary`。**注意**：本机 Git Bash（PortableGit）即使行尾正确也无法执行 `gradlew` —— 它不会为无扩展名的 `java` 自动补 `.exe`，属环境限制；已把工作区 `gradlew` 修为 LF 并验证 CRLF 阻碍消除，本环境请继续用 `./gradlew.bat` |
| 3.2 | ~~补 seat 模块 proguard 文件~~ ✅ | 补上 `proguard-rules.pro` 与 `consumer-rules.pro`。`build.gradle` 早已引用这两个文件，只是文件不存在（`minifyEnabled false` 下不报错，release 会踩坑）。`consumer-rules.pro` 刻意留空并注明原因（模块内无反射入口） |
| 3.3 | ~~收敛调试日志~~ ✅ | 新增 `SeatLog`：按 `BuildConfig.DEBUG` 开关（需在 `build.gradle` 显式启用 `buildConfig true`，AGP 8 起 library 默认关闭），并提供接受 lambda 的重载避免 release 下拼字符串。44 处 `Log.*` 全部改经此出口 |
| 3.4 | ~~抽取重复 CookieJar~~ ✅ | 新增 `SeatCookieJar`，原先 `SeatApi`/`SeatSession`/`SeatCasLogin` 三处各写一遍的实现合并为一；顺带统一了此前有分歧的细节（`maxAge` 类型、`domain`/`path` 兜底、`version`） |
| 3.5 | ~~复用 OkHttpClient~~ ✅ | 新增 `SeatHttp` 统一提供客户端：`trySilentAuth()` 原先**每次调用**都新建 OkHttpClient（连接池、线程池全部白建），现在登录类请求共用一个 `session` 实例，业务客户端从 `base()` 派生并追加认证拦截器 |
| 3.6 | ~~补单元测试~~ ✅ | 新增 4 个测试类共 **31 条**用例：任务 JSON 序列化与容错、任务状态机（终态保护 / 批量终止 / `loadOnce` 合并语义）、CAS 响应解析（含 `member` 为对象与数组两种形态）。`testDebugUnitTest` 通过 |

### 附带的结构简化

`SeatCasLogin` 已被**删除**。它在重构后只剩两个纯转发的薄方法 ——
`syncWebViewCookies()` 归入 `SeatHttp`（cookie 桥的自然归属），
`trySilentAuth()` 归入 `SeatSession`（会话管理的自然归属）。
同时移除了 `SeatSession.jwtToken`（冗余副本，从未被读取，权威 token 在 `SeatApi`）。

---

## M4 体验优化

| # | 任务 | 状态 |
|---|------|------|
| 4.1 | ~~日期选择扩展~~ ✅ | 日期选项改为读取 `/api/Seat/date` 返回的可约日期（横向滚动 chip，今天/明天显示为「今天」「明天」，其余显示日期），接口未返回时回落到「今天/明天」 |
| 4.2 | ~~座位图交互优化~~ ✅ | 抽出 `SeatColors` 作为配色与文案的唯一来源（此前图例与座位格各硬编码一份颜色、文案还不一致），图例补「已选中」并改用 `FlowRow` 自动换行；加载失败增加「重试」按钮；标题栏增加「换区域」直达入口 |
| 4.3 | ~~预约成功 / 失败本地通知~~ ✅ | **已由 M2.3 交付**（独立渠道 `seat_result`，点击回应用） |
| 4.4 | ~~前台服务 + 常驻通知~~ ✅ | **已由 M2.3 交付**（`SeatMonitorService`，`dataSync` + 常驻通知） |

> M4.3 与 M4.4 原列为独立事项，实际与 M2.3「后台保活」是同一件事的两个侧面，已随 M2.3 一并完成
> —— 保留在表中是为了说明去处，不再重复实施。

---

## 后续

路线图上的 **M1–M9 均已完成**。真机联调已完成首轮（预约 → 取消全链路跑通）。

**A. 剩余真机验证** —— 见文末「真机验证清单」，逐项确认。

**B. 候选开发方向**

- 预约历史记录（当前只有任务列表，完成后即长期驻留，无归档/清理）
- 多座位批量预约（同一时段抢多个候选座位）
- Release 构建：目前仍未开混淆，正式分发前应打开并验证 proguard 规则
- 失败重试策略细化（区分网络错误与业务错误，后者不该退避重试）

---

## 风险与前提

- ~~**验收必须用真机 + school WiFi**：模拟器被学校防火墙挡在 seatlib 之外~~
  **⚠️ 该结论已被推翻（2026-09-18 实测）**：模拟器 `Pixel_6_API_34` 到
  `seatlib.bit.edu.cn`（10.0.11.162:443）**TCP 建连成功**，DNS 也能正确解析。
  对照组（关闭端口、不可达 IP）均超时，结论可信。**模拟器可以用于联调**。
- 但 App 的「座」页被外层 `WithLoginStatus` 按 **BIT101 学校登录**门禁拦住，
  因此在模拟器上验证座位功能**仍需先完成学校账号登录**（需要本人凭据）。
  ⚠️ 注意 seatlib 是**单会话**，同账号别处登录会互踢；且短时间多次登录会触
  学校 SSO 风控（要求二次验证）—— 验证时避免 App 与脚本同时登录。
- 轮询频率与学校服务端的容忍度未知。退避已加（基准 10s / 5s，连续失败 2 倍退避封顶 5 分钟），
  但首次真机测试仍建议观察是否触发限流。
- 模拟器上「卡死成 offline」时（`qemu` 进程还在但 `adb shell` 全挂），
  只能强杀后 `-no-snapshot-load` 重启，`adb kill-server` 救不回来。
- **耗电**：5-10 秒持续轮询 2 小时，电量消耗会很明显，这是「能抢到座」的必要代价。
- **Android 15 对 `dataSync` 前台服务有 6 小时/天上限**：单任务上限 2 小时在限内，
  但连续挂多个任务可能触顶。

## 已实测的接口契约（2026-09-18）

以下均来自对 `seatlib.bit.edu.cn` 的真实请求，**不是推测**：

| 接口 | 需要认证 | 未认证时的响应 |
|------|---------|---------------|
| `POST /api/Seat/tree` | 否 | 200 + 真实数据 |
| `POST /api/Seat/date` | 否 | 200 + 真实数据 |
| `POST /api/Seat/seat` | 否 | 200 + 真实数据 |
| `POST /api/Seat/confirm` | **是** | **200** + `{"code":10001,"message":"您尚未登录"}` |
| `POST /api/Space/cancel` | **是** | **200** + `{"code":10001,"message":"您尚未登录"}` |
| `POST /api/cas/user` | — | 200 + `{"code":0,...,"member":[]}` |

由此修正了三个原本会导致**静默失效**的假设：

1. **认证失败不是 HTTP 401，而是 200 + 业务码 10001。** 按 401 判定的实现
   永远发现不了 token 失效，用户只会看到「预约失败」而不会被引导重新登录。
2. **座位号是补零字符串**（`"001"`），用户输入 `"1"` 时 `no == seatNo` 永远不匹配，
   监控任务会一直显示「未找到座位」却不报错。
3. **`times[].id/start/end` 恒为 null**，所以代码里 `"1"/08:00/22:30` 的回落
   是**正常路径**而非边界情况。

其余实测细节：`type` 字段在 JSON 里是**字符串** `"1"`（而 `isValid` 有时字符串有时数字）；
座位 `status` 是字符串 `"1"`=可约 / `"2"`=我已预约 / 其它=占用；
`/api/Seat/date` 当前只返回今天与明天两天。

## 真机验证清单

按重要性排序，建议逐项确认（✅ = 已完成实测）：

1. ✅ ~~三种模式实际预约行为~~ —— 监控 / 优先模式已接线并实测（M1.1 / M5.2，座位改为图上点选）
2. ⏳ **退避是否生效** —— 关掉 WiFi，观察任务消息里的倒计时是否 20s → 40s → 80s 递增
3. ⏳ **后台保活** —— 退后台 / 锁屏 / 杀进程重开，监控是否继续
4. ⏳ **结果通知** —— 抢到座位时是否有通知，点击能否回到应用；拒绝通知权限时是否出现提示条
5. ⏳ **登录态** —— 冷启动是否免重登；用失效 token 触发后 UI 是否回到登录态并给出提示
6. ✅ ~~多日预约~~ —— 日期 chip 已改为读取服务端返回的可约日期（M4.1）

> 已完成：预约 → 取消全链路（真实预约成功 → App 内取消成功）、
> 「我的预约」签到时限与取消次数、座位图与官网逐像素一致性。

---

## M5 UI 优化（2026-09-19，用户反馈驱动）✅ 已完成

| # | 任务 | 说明 |
|---|------|------|
| 5.1 | 底栏压缩 | 删掉模块自造的 `NavigationBar`（两个带图标 item≈80dp，叠在全局底栏之上），改 32dp 胶囊分段控件；座位图详情页隐藏 |
| 5.2 | 图上点选座位 | 监控/优先不再手输座位号：监控图上点选（占用座位也可点），优先多选并按优先级排队，可「不限」 |
| 5.3 | 真实座位底图 | 接入 `/api/seat/map`（free/book/close/leave/use 五张 1920×1080 底图）+ 座位百分比坐标，座位图改为「房间图 + 热区 + 双指缩放」，初始自动缩放到座位密集区；无底图区域回落密集方格并按座位号排序 |
| 5.4 | 我的预约 | 列表页新增「我的预约」（`/api/index/subscribe`）：签到时限提醒（当日 60 分钟内 / 次日 9:00 前）、取消与当天剩余次数提示（规则每天 2 次） |
| 5.5 | 认路与选区域 | 选楼层后显示官方楼层平面图（可放大）；区域由下拉改为芯片 |

## M6 座位图四项体验修复（2026-09-19/20，用户反馈驱动）✅ 已完成

| # | 任务 | 说明 |
|---|------|------|
| 6.1 | 布局继续压缩 | 自绘顶栏 48dp → **40dp**（废 `TopAppBar` 64dp）；信息条改 `FlowRow` 一屏全显；底部操作条移入 `bottomBar` 槽位紧贴底边 |
| 6.2 | 底栏空隙根因修复 | ⚠️ 空隙**不是**圆角造成的 —— 本模块嵌在 `IndexScreen` 的 `Scaffold` 内，而全局底栏（80dp）用 `padding(bottom=)` 给 `NavHost` 让位、**没有消费 window insets**，导致内层 `Scaffold` 的 `paddingValues` 底部凭空多出约 48dp。修法：详情页传 `contentWindowInsets = WindowInsets(0)` |
| 6.3 | 「立即预约」灰态无反馈 | 未选座位时保持可点，点下去弹 Snackbar 提示（原来灰态点击毫无反馈，像卡住） |
| 6.4 | 视口几何抽纯函数 | `ViewportState` / `initialViewport()` / `applyTransform()` 抽为可测纯函数 + 14 条单测（**双指缩放无法用 adb 模拟**，此前从未被真正验证过） |

### 初始缩放的四轮演进（全部依用户实测反馈，**勿擅自回退**）

| 轮 | 方案 | 结果 |
|---|---|---|
| 一 | `scale=1` 按宽度贴合 | 图只占屏高 1/3、上下大留白 → 用户「布局比较奇怪」 |
| 二 | 纵向铺满（约 3 倍） | 无留白，但横向只剩房间 1/3 宽、认路要拖 → **用户否决** |
| 三 | 回到整图可见 | 图太小仍觉奇怪 → **用户仍不满意** |
| **四（定稿）** | **折中 1.8 倍** | 图占屏高约 60%，纵向留白 318px → 75px，横向各裁图宽 11%，**用户选定** |

---

## M7 座位图叠图致命 bug + ANR（2026-09-20，用户报 bug 后查明）✅ 已完成

这是本项目**最严重的一次理解错误**，推翻了 M5.3 的做法。

| # | 问题 | 修复 |
|---|------|------|
| 7.1 | **五张底图不是互补图层，是五张各自完整的房间图** —— 全叠后 `leave` 在最上层，导致**所有座位都显示成时钟（临时离开）** | 改为**每个座位按 status 挑一张图、只显示那一块**（对齐官方 `chunk_seat-map.js` 的 `S` 函数） |
| 7.2 | 改为「每座位一个 `AsyncImage`」后在 103 座位区域触发 **ANR**（主线程卡死 5 秒）—— 100+ 节点 / 请求 / 位图 | 改为**按状态去重（≤5 张）+ 单个 `Canvas` 逐座位裁剪绘制**。请求 103 → 5，组合节点 103 → 2 |
| 7.3 | 顺带修真缺陷 | `SeatTaskRepository.persist()` 在 `loadOnce()` 之前写盘会**抹掉磁盘上已有的历史任务** |

**教训**：官方前端源码是最终权威；「看起来对」≠「对」；修完功能要立刻看性能，
且性能测试必须用**最大**的数据集（21 座位正常，103 座位直接 ANR）。

---

## M8 座位图状态图标完全不显示（2026-09-20）✅ 已完成

| # | 问题 | 修复 |
|---|------|------|
| 8.1 | 用户在**视听学习空间**（徐特立馆 3 楼，状态齐全的区域）复测仍不对 | 根因：`drawSeatTile` 里 `withTransform{translate(-x,-y)}` 内的 `clipRect` **也被同一变换**，实际裁的是画布 `(0,0,w,h)` 而图被平移到 `(-x,-y)` 起画 → 两者错开、瓦片被裁没。改用 `drawImage` 的 `srcOffset`/`srcSize` 源图裁剪 |
| 8.2 | `free` 底图本身「所有座位都画成绿色」 | 它是「全绿底版」，所以逐座位覆盖必须**每个都成功**；位图未就绪时**整块不画**（「全有或全无」），避免半成品全绿画面 |
| 8.3 | **验收方式升级** | 不再靠肉眼看截图：按官方 `S` 函数公式 + 真实坐标重绘基准图，与 App 截图**逐像素差分**。实测平均差 **0.0211**、差>0.40 仅 **1.16%**（残差为 JPEG 伪影）= 与官网渲染一致 |

**教训**：「画了 103 个瓦片」≠「瓦片画对了位置」；验证必须落到**像素**上；
测试要用**用户实际提的那个数据集**（此前一直测状态单一的「智汇轩」，掩盖了问题）。

---

## M9 预约任务列表页体验优化（2026-09-20，用户选 C 档）✅ 已完成

> 用户选定：**连结构一起调整** + 显示**尝试次数与最近尝试时间** + 终态**折叠进「已结束」分组**。

| # | 任务 | 说明 |
|---|------|------|
| 9.1 | **结构重排** | 「我的预约」与「预约任务」是**因果**关系（任务成功后变成一条预约），不是并列。故任务区默认**只显示进行中**，终态收进可展开的「已结束 N」分组，避免几十条死任务把真正在跑的埋掉 |
| 9.2 | **排序规则** | 一级键「**是否进行中**」，二级 `createdAt` 倒序，三级 `id` 兜底（防同秒创建抖动）。⚠️ 不能只按 `createdAt` 排 —— 三天前建立、仍在跑的监控任务会被今天新建的、已失败的终态任务压到下面 |
| 9.3 | **三层「存活证据」** | ①已等待 N（`createdAt` 算，老数据缺失则隐藏）②尝试 N 次（**永远显示，含 0** —— 「尝试 0 次」本身就是异常信号）③最近尝试 N 前（无记录显示「尚未尝试」而非静默省略）。抽成 `TaskListLogic.livenessText` 纯函数以便单测覆盖 |
| 9.4 | 模型/仓库扩展 | `ReservationTask` +`createdAt`/`attempts`/`lastAttemptAt`（JSON 双向，老数据回落 0，防「1970 年 → 已等待 56 年」）；仓库 +`markAttempt`（**终态不累加**）/`clearFinished`/`remove` |
| 9.5 | UI 细节 | 下拉刷新（本项目 material3 为 1.2.0-rc01，**无 `PullToRefreshBox`**，用旧式 `PullToRefreshContainer`）；签到倒计时（**恰好到点即判超时**，不显示「还剩 0 分钟」）；单一 `nowTick` 每秒驱动所有相对时间 |

**单测 89 → 123 条**（新增 `TaskListLogicTest` 等 34 条）。

---

## 长期愿景（用户提出，暂不做）

- **3D 图书馆**：用座位坐标（point_x/point_y 百分比）与楼层平面图做三维导览，
  直观展示「哪片区域还有空位」。工作量集中在建模与渲染，属独立项目；
  当前已拿到的坐标与平面图正好是它的地基数据。
